require 'spec_helper'

describe Omniauth::Medium do
  it 'has a version number' do
    expect(Omniauth::Medium::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
